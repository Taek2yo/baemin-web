/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['baemin-taek.s3.amazonaws.com', "avatars.githubusercontent.com"],
      },
}

module.exports = nextConfig
